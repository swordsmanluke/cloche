# New Bead Task

Create a new bead task. Use `bd create` to add new tasks. If you don't recall the syntax, run `bd create --help` to look it up.

## Task

$ARGUMENTS

## Input validation

if the task section is empty, abort and alert the User.

## Creation

Create the task (or tasks) as outlined by the user, using bead to create them.
