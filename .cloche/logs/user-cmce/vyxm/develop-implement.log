All tests pass. Here's a summary of what was implemented:

### Changes Made

**1. `internal/domain/run.go`** — Added `ContainerKept bool` field to the `Run` struct to track whether a container was retained after run completion.

**2. `internal/adapters/sqlite/store.go`** — Three changes:
   - Added `container_kept INTEGER NOT NULL DEFAULT 0` column migration
   - Updated all SQL queries (`CreateRun`, `GetRun`, `UpdateRun`, `ListRuns`, `ListRunsByProject`, `ListRunsSince`) to include the new column
   - Added `boolToInt` helper for SQLite boolean serialization

**3. `internal/adapters/grpc/server.go`** (lines 269-290) — Replaced the simple `keepContainer` check with the retention policy:
   - **Failed/cancelled runs**: always keep the container and set `ContainerKept = true`
   - **Successful runs + `--keep-container`**: keep the container and set `ContainerKept = true`
   - **Successful runs without `--keep-container`**: remove the container (existing behavior)

**4. Tests added:**
   - `TestRunContainerKept` in `store_test.go` — verifies `ContainerKept` round-trips through SQLite (create, update, list)
   - `TestServer_RunWorkflow_FailedRunKeepsContainer` — verifies failed runs keep their container and set `ContainerKept = true`
   - `TestServer_RunWorkflow_SucceededRunRemovesContainer` — verifies successful runs remove their container
   - `TestServer_RunWorkflow_KeepContainerOnSuccess` — verifies `--keep-container` keeps container on success

CLOCHE_RESULT:success
