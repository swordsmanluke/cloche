All tasks complete. Here's a summary of what was done:

**Created `docs/INSTALL.md`** covering these installation methods:
- **Build from source** — `make install` with configurable `PREFIX`
- **`go install`** — install binaries directly via `go install github.com/cloche-dev/cloche/cmd/...@latest`
- **Pre-built release binaries** — download from GitHub Releases
- **Homebrew tap** — `brew tap cloche-dev/tap && brew install cloche`
- **Docker-only usage** — running the daemon in a container for CI or evaluation
- **Verification** and **upgrading** instructions

**Updated `docs/USAGE.md`** — added a link to `INSTALL.md` in the Prerequisites section so users can easily find detailed installation options.

All existing tests pass.

CLOCHE_RESULT:success
