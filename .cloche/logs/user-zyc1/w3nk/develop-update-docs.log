The file looks good. All discrepancies have been addressed. Let me do a final review to make sure I haven't missed anything or introduced errors.

Summary of changes made to `docs/USAGE.md`:

1. **`cloche run`** — Added `--keep-container` to usage line and flag table
2. **`cloche list`** — Changed description to "List runs (last hour by default)", added `--all` flag table, fixed column description from "start time" to "container ID (if any), error message (if any)"
3. **Added missing CLI Reference entries** — `cloche init` (with `--workflow`/`--image` flags), `cloche logs`, `cloche poll`, `cloche shutdown`
4. **Workflow template** — Added `update-docs` step and its wiring (`test:success -> update-docs`, `update-docs:success/fail -> done`) to match current `cloche init` output
5. **Project layout** — Added `update-docs.md` to the prompts directory listing
6. **Daemon config** — Added `CLOCHE_HTTP` env var
7. **Build commands** — Added `make install`
8. **Auto-rebuild** — Documented automatic Dockerfile staleness detection and rebuild in the Container Isolation Model section

CLOCHE_RESULT:success
