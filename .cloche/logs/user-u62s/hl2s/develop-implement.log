All tests pass. Here's a summary of what was done:

**Created `docs/INSTALL.md`** — Comprehensive installation guide covering:
- **Prerequisites** (Docker, Git, API key)
- **Build from source** (`make build` / `make install` with PREFIX override)
- **`go install`** (direct from module path, with Docker image note)
- **Pre-built release binaries** (platform table, extraction commands)
- **Homebrew tap** (`brew tap cloche-dev/tap && brew install cloche`)
- **Docker-only usage** (host socket mount for running daemon in a container)
- **Verification steps** and **PATH setup** guidance

**Updated `docs/USAGE.md`** — Added a link to INSTALL.md in the Prerequisites section so users can discover all installation options.

CLOCHE_RESULT:success
