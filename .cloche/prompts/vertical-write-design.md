# Write design doc (stub)

Write a minimal design document for this feature. Create a file at
`docs/plans/<YYYY-MM-DD>-<feature-slug>.md` with the following content:

```markdown
# <Feature title>

**Date:** <today>
**Status:** Draft

## Problem

<brief problem statement>

## Solution

<brief solution sketch>
```

Commit the file with the message: "Add design doc for <feature-id>".

Then output:

```
CLOCHE_RESULT:success
```

<!-- TODO: L2 — replace with a real design-writing prompt that reads task
description, researches the codebase, and writes a thorough design doc
following the project's design doc conventions. -->
