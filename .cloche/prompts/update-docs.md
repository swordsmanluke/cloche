The file is fully corrupted — it contains only meta-commentary about itself being corrupted, with zero actual prompt instructions. I'm trying to replace it with the clean prompt content (the `# Update Documentation` header, process steps, rules) plus a learned rule documenting the 5+ cycle corruption pattern. Could you approve the write?