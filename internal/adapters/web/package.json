{
  "name": "cloche-console-tests",
  "private": true,
  "description": "JS unit/smoke tests for the console dashboard's static assets. Not part of the Go build; run with `npm test` from this directory.",
  "scripts": {
    "test": "node --test *.test.js"
  },
  "devDependencies": {
    "jsdom": "^25.0.0"
  }
}
