# Implement Task

`calc.py` has a bug: `add(a, b)` returns `a - b` instead of `a + b`. Fix it
so `add(2, 3)` returns `5`. Edit `calc.py` only.

## Result Selection
When you are finished, output exactly one of the following on its own line:
CLOCHE_RESULT:{nonce}:success
CLOCHE_RESULT:{nonce}:fail
