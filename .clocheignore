# Runtime state — excluded from container copies so agents don't see stale
# prompt files or logs from previous runs.
.cloche/runs/
.cloche/logs/

# Unix sockets cannot be archived into containers.
*.sock
